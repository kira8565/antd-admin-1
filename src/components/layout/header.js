import React from 'react';
import styles from './header.less';

const Header = (props) => {
  return (
    <header>
      Header
    </header>
  );
};

Header.propTypes = {
};

export default Header;
